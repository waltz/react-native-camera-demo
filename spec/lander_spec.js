import assert from 'assert';
// import Lander from '../lander.js';

describe('<Lander/>', () => {
  describe('render()', () => {
    it('renders', () => {
      const lander = 1;
      assert.equal(lander, 2);
    });
  });
});
